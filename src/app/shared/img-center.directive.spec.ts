import { ImgCenterDirective } from './img-center.directive';

describe('ImgCenterDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgCenterDirective();
    expect(directive).toBeTruthy();
  });
});
